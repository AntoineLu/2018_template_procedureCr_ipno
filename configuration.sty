\ProvidesPackage{configuration}
% CONFIGURATION DU DOCUMENT
% On complétera ici les méta-données, titre et auteurs ainsi que les autres configurations de la page ainsi que les fichiers à inclure

% Liste des fichiers à inclure (par défaut tous)
\def\biblio{1} % inclure la bibliographie? 1 oui, 0 non.
\newcommand*{\nombibliographie}{mabiblio.bib} % Le chemin et nom du fichier de bibliographie (ici mabiblio.bib)
\def\annexes{1} % inclure les annexes? 1 oui, 0 non.

% Écrire ici le titre, l'auteur et la date de publication... Écrire N.A. si non applicable.
\newcommand*{\type}{<Type du document>}% type de document procès-verbal, procédure, rapport technique...
\newcommand*{\titre}{<Titre du document>}
\newcommand*{\redacteur}{<Prénom et nom du rédacteur>}
\newcommand*{\mailRedacteur}{<Adresse email du rédacteur>}
\newcommand*{\verificateur}{<Prénom et nom du vérificateur>}
\newcommand*{\mailVerificateur}{<Adresse email du vérficateur>}
\newcommand*{\AQ}{<Prénom et nom du chargé d'assurance qualité>}
\newcommand*{\mailAQ}{<Adresse email du chargé d'assurance qualité>}
\newcommand*{\diffusion}{<Diffusion interne ou externe>}
\newcommand*{\datee}{<Date, soit la commande \today (date du jour) soit la date au format souhaité>}
\newcommand*{\version}{<Numéro de la version>}
\newcommand*{\atrium}{<Indentifiant Atrium>}
\newcommand*{\vv}{}
\newcommand*{\reference}{<Référence>}
\endinput
